[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci"]

# Note: We don't build the frontend here, only install dependencies
# The server runs directly with tsx

[start]
cmd = "npm run server"
